import type { Metadata } from "next"; import Link from "next/link"; import {notFound} from "next/navigation"; import {PageHero} from "@/components/PageHero"; import {getDictionary} from "@/lib/dictionaries"; import {hasLocale,localePath} from "@/lib/i18n";
export async function generateMetadata({params}:PageProps<"/[locale]/about">):Promise<Metadata>{const{locale}=await params;if(!hasLocale(locale))return{};return{title:getDictionary(locale).nav.about,description:getDictionary(locale).about.intro};}
export default async function AboutPage({params}:PageProps<"/[locale]/about">){const{locale}=await params;if(!hasLocale(locale))notFound();const d=getDictionary(locale),a=d.about;return <><PageHero title={a.title} intro={a.intro}/><section className="section"><div className="shell editorial-grid"><h2>{a.values.split("\n").map((line)=><span className="value-line" key={line}>{line}</span>)}</h2><div className="prose"><h3>{a.philosophyTitle}</h3><p>{a.philosophyBody}</p><h3>{a.priorityTitle}</h3><p>{a.priorityBody}</p><div className="unverified-inline">{a.pending}</div><Link className="button button-primary" href={localePath(locale,"/products")}>{d.common.viewProducts}</Link></div></div></section></>;}
