import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{PageHero}from"@/components/PageHero";import{getDictionary}from"@/lib/dictionaries";import{hasLocale,localePath}from"@/lib/i18n";
export async function generateMetadata({params}:PageProps<"/[locale]/distributors">):Promise<Metadata>{const{locale}=await params;if(!hasLocale(locale))return{};const d=getDictionary(locale);return{title:d.nav.distributors,description:d.distributors.intro};}
export default async function DistributorsPage({params}:PageProps<"/[locale]/distributors">){const{locale}=await params;if(!hasLocale(locale))notFound();const d=getDictionary(locale),x=d.distributors;return <><PageHero title={x.title} intro={x.intro}/><section className="section"><div className="shell partner-layout"><div><h2>{x.materialsTitle}</h2><ul className="plain-list">{x.materials.map((item)=><li key={item}>{item}</li>)}</ul></div><aside className="partner-status"><strong>{x.statusTitle}</strong><h3>{d.common.documentsPreparing}</h3><p>{x.statusBody}</p><Link className="button button-secondary" href={localePath(locale,"/contact")}>{d.common.contact}</Link></aside></div></section></>;}
